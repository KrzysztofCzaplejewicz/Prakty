@using System.Collections.Generic
@model System.Collections.Generic.IEnumerable<FutbolAmerykanski.Models.Liga>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Grade)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.DruzynyModel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Zawodnik)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Grade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DruzynyModel.IdDruzyny)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Zawodnik.IdZawodnik)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LigaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LigaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LigaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
